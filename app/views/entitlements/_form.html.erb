<%= form_with(model: entitlement, local: true) do |form| %>
  <% if entitlement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entitlement.errors.count, "error") %> prohibited this entitlement from being saved:</h2>

      <ul>
      <% entitlement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :product_license_id %>
    <%= form.text_field :product_license_id %>
  </div>

  <div class="field">
    <%= form.label :valid_from %>
    <%= form.datetime_select :valid_from %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
