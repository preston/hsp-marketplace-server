header.navbar.navbar-custom.navbar-fixed-top
  .container-fluid

  	  / Brand and toggle get grouped for better mobile display
	  .navbar-header.page-scroll
	    button.navbar-toggle type="button" data-toggle="collapse" data-target="#navbar-collapse-1"
	      span.sr-only Toggle navigation
	      span.icon-bar
	      span.icon-bar
	      span.icon-bar
	  
	    a.navbar-brand href="#{root_url}"
	      |  Marketplace

	  nav#navbar-collapse-1.collapse.navbar-collapse
	    ul.nav.navbar-nav
	      - if user_signed_in?
	        li
	          = link_to('Dashboard', dashboard_path)
	    
	    /   li.dropdown
	        a.dropdown-toggle data-toggle="dropdown"
	          | Learn
	          b.caret
	        ul.dropdown-menu role="menu"
	            li = link_to 'Bug Reports', 'https://github.com/preston/marketplace-server/issues'
	            li = link_to 'Feature Requests', 'https://github.com/preston/marketplace-server/issues'
	            li = link_to 'Source Code', 'https://github.com/preston/marketplace-server'

	    ul.nav.navbar-nav.navbar-right
	    	/ - if user_signed_in?
	    	/ - if can? :manage, User
	          li.dropdown
	            a.dropdown-toggle data-toggle="dropdown"
	              | Admin
	              b class="caret"
	            ul.dropdown-menu
	        		li = link_to text_with_icon('Manage users.', 'users'), users_path
					li class="divider"
  	        		li = link_to 'IDPs', providers_path

	        li

		        a href="#{edit_user_path(current_user)}"
			    	img.gravatar src="#{gravatar_image_url(current_identity.email, filetype: :png, rating: 'pg', size: 20, secure: true)}"
	            	|  
	            	= current_user.name
	        li = link_to('Logout', logout_path, method: :delete,)
